import { ValidarEspaciosDirective } from './validar-espacios.directive';

describe('ValidarEspaciosDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidarEspaciosDirective();
    expect(directive).toBeTruthy();
  });
});
